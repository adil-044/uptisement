/**
 * Uptisement tracking config — paste real IDs, then push.
 * Leave a field "" to skip that vendor.
 */
window.UPTI_TRACK = {
  ga4: "", // e.g. "G-XXXXXXXXXX"
  metaPixel: "", // e.g. "123456789012345"
  clarity: "", // optional Microsoft Clarity project id
  linkedinPartner: "", // optional LinkedIn Insight partner id
  gtm: "", // optional "GTM-XXXXXXX" — if set, GTM owns tags; still fire dataLayer events
};
